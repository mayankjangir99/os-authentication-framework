{
  "name": "secure-auth-os-web",
  "version": "1.0.0",
  "description": "Web UI and MongoDB backend for the secure authentication OS project.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.4.1"
  },
  "engines": {
    "node": ">=18"
  }
}
